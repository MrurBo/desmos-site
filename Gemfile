source "https://rubygems.org"

# Core Jekyll
gem "jekyll", "~> 4.3"

# GitHub Pages compatible dependencies (important for consistency with GH Pages)
gem "github-pages", group: :jekyll_plugins

# Default Jekyll theme (optional but commonly used)
gem "minima", "~> 2.5"

# Useful for local development and builds
gem "webrick", "~> 1.8"